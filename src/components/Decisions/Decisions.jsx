import styles from './Decisions.module.scss';

const Decisions = () => {
	return (
		<div>
			<h2 className={`${styles.title} title`}>
				<span>Решения</span> по отраслям
			</h2>
		</div>
	);
};

export default Decisions;
